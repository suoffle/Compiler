1. prac.l 파일 오타 수정(수정 완료)  
   - break		{return(BRAEK_SYM);}   ->   break		{return(BREAK_SYM);}

2. prac.y 파일 오타 수정(수정 완료)  
   - 맨 처음 부분 '%{' 부분 아래에 #include <stdio.h> 추가.
   - puts 함수를 사용 도중 <stdio.h>가  없어 경고 메세지가 발생하였다.
